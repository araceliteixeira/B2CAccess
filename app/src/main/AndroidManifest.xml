<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.araceliteixeira.b2caccess">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" android:label="B2C ACCESS">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserView" android:label="PRODUCTS WITH DISCOUNT" />
        <activity android:name=".GuestView" android:label="PRODUCTS WITH NO DISCOUNT" />
        <activity android:name=".NewUser" android:label="B2C NEW USER" />
    </application>

</manifest>